# 🔧 Версия 6.2 - Детальная отладка и правильная конвертация naming

## ✅ Что исправлено

### 1. Правильная конвертация camelCase → snake_case

**Было:**
```typescript
orderType → _order_type  ❌
productId → _product_id  ❌
regNumberS → _reg_number_s ❌
```

**Стало:**
```typescript
orderType → order_type  ✅
productId → product_id  ✅
regNumberS → reg_number_s ✅
```

**Протестировано на 12 кейсах - все работает!**

### 2. Максимально детальная отладка

Теперь видите **каждый шаг** поиска:

```
📊 ЭТАП 1: Анализ схемы БД...
  🔍 Ищу таблицы для полей: orderType, productId, regNumberS

  📋 SQL запрос для получения схемы БД:
  ┌─────────────────────────────────────────────────────────────────┐
  │   SELECT                                                          │
  │     table_name,                                                   │
  │     column_name,                                                  │
  │     data_type,                                                    │
  │     is_nullable                                                   │
  │   FROM information_schema.columns                                 │
  │   WHERE table_schema = 'public'                                   │
  │   ORDER BY table_name, ordinal_position                           │
  └─────────────────────────────────────────────────────────────────┘

  ✓ Получено 487 колонок из БД
  
  📊 Примеры колонок из БД (первые 10):
      users.id (integer)
      users.email (character varying)
      users.created_at (timestamp)
      orders.id (integer)
      orders.customer_id (integer)
      orders.order_type (character varying)
      orders.status (character varying)
      order_items.id (integer)
      order_items.product_id (integer)
      order_items.quantity (integer)
      ... и еще 477 колонок

  ✓ Найдено 42 таблиц в БД

  🔎 ДЕТАЛЬНЫЙ АНАЛИЗ КАЖДОГО ПОЛЯ DTO:
  ═══════════════════════════════════════════════════════════════════

  📌 Поле DTO: "orderType"
     Генерирую варианты: orderType, ordertype, order_type, ORDER_TYPE, order-type, OrderType, orderTypes, order_types, ordertypes, order, order_
     ✓ НАЙДЕНО в таблице "orders": order_type

  📌 Поле DTO: "productId"
     Генерирую варианты: productId, productid, product_id, PRODUCT_ID, product-id, ProductId, productIds, product_ids, productids, product, product_, product_id, productid, product_id, productid
     ✓ НАЙДЕНО в таблице "orders": product_id

  📌 Поле DTO: "regNumberS"
     Генерирую варианты: regNumberS, regnumbers, reg_number_s, REG_NUMBER_S, reg-number-s, RegNumberS, regNumberSs, reg_number_ss, regnumberss, regNumber, regnumber, reg_number
     ✓ НАЙДЕНО в таблице "orders": reg_number_s

  ╔═══════════════════════════════════════════════════════════════╗
  ║ 🎯 ТАБЛИЦА: orders                                            ║
  ║ Совпадений: 3/3 (100%)                                        ║
  ╠═══════════════════════════════════════════════════════════════╣
  ║ ✓ orderType → order_type                                      ║
  ║ ✓ productId → product_id                                      ║
  ║ ✓ regNumberS → reg_number_s                                   ║
  ╚═══════════════════════════════════════════════════════════════╝
```

### 3. Диагностика когда ничего не найдено

```
  🔎 ДЕТАЛЬНЫЙ АНАЛИЗ КАЖДОГО ПОЛЯ DTO:
  ═══════════════════════════════════════════════════════════════════

  📌 Поле DTO: "customerId"
     Генерирую варианты: customerId, customerid, customer_id, ...
     ⚠️  НЕ НАЙДЕНО в "orders", но есть похожие:
        - user_id
        - client_id

  📌 Поле DTO: "orderItems"
     Генерирую варианты: orderItems, orderitems, order_items, ...
     ✗ НЕ НАЙДЕНО в "orders"

  ╔═══════════════════════════════════════════════════════════════╗
  ║ ⚠️  СОВПАДЕНИЙ НЕ НАЙДЕНО                                    ║
  ╚═══════════════════════════════════════════════════════════════╝

  🔧 ОТЛАДОЧНАЯ ИНФОРМАЦИЯ:

  📋 Ваши поля DTO:
     - customerId
     - orderItems
     - totalAmount

  🔄 Примеры генерируемых вариантов:
     customerId →
        - "customerId"
        - "customerid"
        - "customer_id"
        - "CUSTOMER_ID"
        - "customer-id"
        - "CustomerId"
        - "customerIds"
        - "customer_ids"
        - "customer"
        - "customer_"
        - "customer_id"
        - "customerid"

  💡 ВОЗМОЖНЫЕ ПРОБЛЕМЫ:
     1. Naming convention отличается от стандартной
     2. Поля находятся в разных таблицах
     3. Имена полей в БД сильно отличаются от DTO

  📝 РЕКОМЕНДАЦИИ:
     1. Проверьте реальные имена колонок в БД:
        SELECT column_name FROM information_schema.columns
        WHERE table_name = 'предполагаемая_таблица';

     2. Сравните с вашими полями DTO:
        DTO: customerId → БД: customer_id
        DTO: orderItems → БД: order_items
        DTO: totalAmount → БД: total_amount

     3. Если naming сильно отличается, используйте force: false
        и укажите таблицы вручную в тесте
```

### 4. Расширенная генерация вариантов

Теперь генерируется **10-13 вариантов** для каждого поля:

```typescript
// orderType генерирует:
[
  'orderType',        // Оригинал
  'ordertype',        // lowercase
  'order_type',       // snake_case ✅
  'ORDER_TYPE',       // SCREAMING_SNAKE_CASE
  'order-type',       // kebab-case
  'OrderType',        // PascalCase
  'orderTypes',       // plural
  'order_types',      // snake_case plural
  'ordertypes',       // lowercase plural
  'order',            // без суффикса Type
  'order_'            // snake без суффикса
]

// productId генерирует:
[
  'productId',
  'productid',
  'product_id',       // ✅
  'PRODUCT_ID',
  'product-id',
  'ProductId',
  'productIds',
  'product_ids',
  'productids',
  'product',          // без Id
  'product_',         // snake без Id
  'product_id',       // альтернатива для *Id
  'productid',
  'product_id',
  'productid'
]

// regNumberS генерирует:
[
  'regNumberS',
  'regnumbers',
  'reg_number_s',     // ✅
  'REG_NUMBER_S',
  'reg-number-s',
  'RegNumberS',
  'regNumberSs',
  'reg_number_ss',
  'regnumberss',
  'regNumber',        // без последней S
  'regnumber',
  'reg_number'
]
```

### 5. Утилита для тестирования

Создан файл `test-naming.ts` для проверки конвертации:

```bash
cd api-generator
npx ts-node test-naming.ts
```

Результат:

```
🧪 ТЕСТИРОВАНИЕ КОНВЕРТАЦИИ NAMING

📌 DTO поле: "orderType"
   Ожидается: "order_type"
   Результат: ✅ НАЙДЕНО

📌 DTO поле: "productId"
   Ожидается: "product_id"
   Результат: ✅ НАЙДЕНО

📌 DTO поле: "regNumberS"
   Ожидается: "reg_number_s"
   Результат: ✅ НАЙДЕНО

... все 12 тестов прошли ✅
```

## 🎯 Как использовать новую отладку

### 1. Запустите анализ

```typescript
import { analyzeAndGenerateTestData } from 'openapi-typescript-generator';
import { testDbConnect } from './helpers/dbConnection';

await analyzeAndGenerateTestData(
  {
    testFilePath: './tests/api/orders/createOrder.test.ts',
    dbConnectionMethod: 'testDbConnect',
    force: true,
    dataStrategy: 'existing'
  },
  testDbConnect
);
```

### 2. Изучите детальный вывод

Смотрите секцию **"ДЕТАЛЬНЫЙ АНАЛИЗ КАЖДОГО ПОЛЯ DTO"**:

- Какие варианты генерируются для каждого поля
- Найдено ли совпадение в таблице
- Если не найдено - какие похожие колонки есть

### 3. Проверьте SQL запрос

Анализатор показывает точный SQL который используется:

```sql
SELECT 
  table_name,
  column_name,
  data_type,
  is_nullable
FROM information_schema.columns
WHERE table_schema = 'public'
ORDER BY table_name, ordinal_position
```

Можете скопировать и выполнить вручную для проверки.

### 4. Проверьте реальные имена в БД

Если ничего не найдено, анализатор предложит:

```sql
SELECT column_name 
FROM information_schema.columns
WHERE table_name = 'orders';
```

Выполните это вручную и сравните с вашими DTO полями.

## 🔍 Примеры проблем и решений

### Проблема: "НЕ НАЙДЕНО, но есть похожие"

```
📌 Поле DTO: "customerId"
   Генерирую варианты: customerId, customerid, customer_id, ...
   ⚠️  НЕ НАЙДЕНО в "orders", но есть похожие:
      - user_id
      - client_id
```

**Решение:** В БД используется `user_id` вместо `customer_id`.

**Варианты:**
1. Переименовать DTO поле на `userId`
2. Или указать таблицу вручную в тесте:
   ```typescript
   const dbTables: string[] = ['orders']; // Вручную
   ```

### Проблема: "Варианты генерируются неправильно"

```
📌 Поле DTO: "mySpecialField"
   Генерирую варианты: myspecialfield, my_special_field, ...
   ✗ НЕ НАЙДЕНО
```

**Решение:** Добавьте кастомные варианты в `generateFieldVariants`:

```typescript
// В database-analyzer.ts
private generateFieldVariants(field: string): string[] {
  const variants = new Set<string>();
  
  // ... существующая логика
  
  // Добавьте кастомные правила
  if (field === 'mySpecialField') {
    variants.add('custom_field_name'); // Ваш кастомный вариант
  }
  
  return Array.from(variants);
}
```

### Проблема: "SQL запрос не возвращает колонки"

```
✓ Получено 0 колонок из БД
✓ Найдено 0 таблиц в БД
```

**Решение:** Проблема с подключением к БД или схемой.

Проверьте:

```typescript
// 1. Подключение работает?
const result = await testDbConnect`SELECT 1`;
console.log(result); // Должно быть [{?column?: 1}]

// 2. Схема public существует?
const schemas = await testDbConnect`
  SELECT schema_name 
  FROM information_schema.schemata
`;
console.log(schemas);

// 3. Таблицы существуют?
const tables = await testDbConnect`
  SELECT table_name 
  FROM information_schema.tables 
  WHERE table_schema = 'public'
`;
console.log(tables);
```

## 📊 Checklist отладки v6.2

- [ ] SQL запрос показывается в логах
- [ ] Получено N колонок из БД (N > 0)
- [ ] Найдено M таблиц в БД (M > 0)
- [ ] Для каждого DTO поля показаны варианты
- [ ] Варианты включают snake_case версию
- [ ] Если не найдено - показаны похожие колонки
- [ ] Финальный результат: найдено совпадений

## ✅ Готово!

Теперь у вас максимальная детализация отладки:
- ✅ Правильная конвертация naming (orderType → order_type)
- ✅ SQL запросы в логах
- ✅ Генерация 10+ вариантов для каждого поля
- ✅ Похожие колонки если ничего не найдено
- ✅ Детальная информация на каждом шаге

Запустите и увидите где именно проблема! 🎊
