# 🎉 OpenAPI TypeScript Generator - Готово к использованию!

Вы получили полноценную библиотеку для генерации TypeScript API клиента из OpenAPI/Swagger спецификаций.

## 📦 Что внутри

1. **api-generator.tar.gz** - Архив с полным исходным кодом генератора
2. **QUICKSTART_RU.md** - Быстрый старт на русском языке
3. **PLAYWRIGHT_EXAMPLE.md** - Детальный пример интеграции с Playwright

## 🚀 Быстрый старт

```bash
# 1. Распаковать архив
tar -xzf api-generator.tar.gz
cd api-generator

# 2. Установить зависимости
npm install

# 3. Собрать проект
npm run build

# 4. Запустить пример (опционально)
npm test
```

## 💡 Основные возможности

### ✅ Что умеет генератор

- **Поддержка всех версий**: OpenAPI 2.0 (Swagger), 3.0 и 3.1
- **Модульная структура**: Отдельный файл для каждого тега
- **Транслитерация**: Автоматическое преобразование русских названий в английские
- **Умное разделение DTO**: Базовые типы в отдельном файле, специфичные - в файлах тегов
- **Type-safe**: Полная типизация TypeScript
- **Готовый HTTP клиент**: Axios с обработкой ошибок из коробки

### 📁 Что генерирует

Из вашего OpenAPI документа создается:

```
generated/
├── index.ts              # Главный файл экспорта
├── http-client.ts        # Настроенный Axios клиент
├── base.types.ts         # Общие DTO (если есть)
├── users.api.ts          # API методы для users
├── products.api.ts       # API методы для products
└── ... другие теги
```

Каждый файл содержит:
- **TypeScript интерфейсы** (DTO) для запросов/ответов
- **Async функции** для вызова API методов
- **JSDoc комментарии** с описанием

## 🎯 Использование в коде

### Базовое использование

```typescript
import { generateApi } from './api-generator/dist/index';

// Генерация API клиента
await generateApi({
  specUrl: 'https://api.example.com/openapi.json',
  outputDir: './src/api/generated',
  httpClient: 'axios',
  baseUrl: 'https://api.example.com/v1',
});
```

### Использование в тестах

```typescript
import { getUserById, createUser } from './api/generated/users.api';
import type { User, UserInput } from './api/generated';

// Создание пользователя
const newUser: UserInput = {
  email: 'test@example.com',
  name: 'Test User'
};

const created = await createUser(newUser);
console.log('Created:', created.id);

// Получение пользователя
const user = await getUserById(created.id);
console.log('User:', user.name, user.email);
```

## 📚 Документация

### Основные файлы

- **QUICKSTART_RU.md** - Подробная инструкция на русском
  - Установка и настройка
  - Примеры конфигурации
  - CI/CD интеграция
  - Troubleshooting

- **PLAYWRIGHT_EXAMPLE.md** - Реальный пример
  - Полная структура проекта
  - Фикстуры для тестов
  - Примеры тестов
  - Best practices

- **README.md** (внутри архива) - Техническая документация на английском
  - API Reference
  - Сравнение с аналогами
  - Детальные примеры

## 🔧 Конфигурация

### Полный список опций

```typescript
interface GeneratorConfig {
  // URL или путь к OpenAPI документу (обязательно)
  specUrl: string;
  
  // Путь для выгрузки файлов (обязательно)
  outputDir: string;
  
  // HTTP клиент (пока только axios)
  httpClient?: 'axios';
  
  // Базовый URL для API запросов
  baseUrl?: string;
  
  // Генерировать обработчики ошибок
  generateErrorHandlers?: boolean;
  
  // Генерировать TypeScript типы
  generateTypes?: boolean;
  
  // Транслитерация русских названий
  transliterateRussian?: boolean;
}
```

## 🎨 Примеры использования

### Пример 1: Локальный файл

```typescript
await generateApi({
  specUrl: './specs/my-api.json',
  outputDir: './src/api',
});
```

### Пример 2: С базовым URL

```typescript
await generateApi({
  specUrl: 'https://api.example.com/openapi.json',
  outputDir: './src/api',
  baseUrl: process.env.API_BASE_URL,
});
```

### Пример 3: С русскими тегами

Если в OpenAPI есть теги:
- "Пользователи"
- "Товары и услуги"
- "Заказы"

Будут созданы файлы:
- `polzovateli.api.ts`
- `tovary-i-uslugi.api.ts`
- `zakazy.api.ts`

## 🆚 Отличия от swagger-typescript-api

| Функция | swagger-typescript-api | Этот генератор |
|---------|------------------------|----------------|
| Модульная структура по тегам | ❌ Один большой файл | ✅ Файл на тег |
| Транслитерация русских названий | ❌ | ✅ |
| Автоматическое разделение базовых DTO | ❌ | ✅ |
| Оптимизация для Playwright | ❌ | ✅ |
| Зависимые типы в файлах | ❌ | ✅ |

## 📖 Структура проекта в архиве

```
api-generator/
├── src/
│   ├── index.ts              # Основной класс генератора
│   ├── parser.ts             # Парсер OpenAPI (2.0, 3.0, 3.1)
│   ├── generator.ts          # Генератор TypeScript кода
│   ├── example.ts            # Пример использования
│   └── utils/
│       ├── transliterate.ts  # Транслитерация
│       └── string-helpers.ts # Хелперы для строк
├── generated/                # Примеры сгенерированных файлов
│   └── test-api/
├── examples/
│   └── playwright-integration.test.ts
├── dist/                     # Скомпилированный код
├── package.json
├── tsconfig.json
├── README.md                 # Английская документация
└── test-swagger.json         # Тестовый OpenAPI файл
```

## 🔄 Workflow для ваших автотестов

### Вариант 1: Генерация перед каждым запуском

```json
{
  "scripts": {
    "generate-api": "node scripts/generate-api.js",
    "pretest": "npm run generate-api",
    "test": "playwright test"
  }
}
```

### Вариант 2: Генерация в CI/CD

```yaml
# .github/workflows/test.yml
- name: Generate API client
  run: npm run generate-api
  
- name: Run tests
  run: npm test
```

### Вариант 3: Ручная генерация при изменении API

```bash
npm run generate-api
git add src/api/generated
git commit -m "Update API client"
```

## ⚙️ Настройка HTTP клиента

После генерации настройте клиент под свои нужды:

```typescript
import { httpClient } from './api/generated/http-client';

// Токен авторизации
httpClient.interceptors.request.use((config) => {
  config.headers.Authorization = `Bearer ${process.env.API_TOKEN}`;
  return config;
});

// Логирование
httpClient.interceptors.request.use((config) => {
  console.log(`→ ${config.method} ${config.url}`);
  return config;
});
```

## 🐛 Решение проблем

### Проблема: Не находит модули

**Решение:** Запустите сборку

```bash
cd api-generator
npm run build
```

### Проблема: Ошибка при генерации

**Решение:** Проверьте доступность OpenAPI документа

```bash
curl -I https://your-api.com/openapi.json
```

### Проблема: Типы не подтягиваются в IDE

**Решение:** Перезапустите TypeScript сервер в IDE

- VSCode: `Ctrl+Shift+P` → "TypeScript: Restart TS Server"
- WebStorm: File → Invalidate Caches

## 📞 Следующие шаги

1. ✅ Распаковайте архив и установите зависимости
2. ✅ Прочитайте QUICKSTART_RU.md
3. ✅ Изучите PLAYWRIGHT_EXAMPLE.md
4. ✅ Сгенерируйте свой первый API клиент
5. ✅ Используйте в тестах!

## 🎉 Готово!

Теперь у вас есть мощный инструмент для работы с API в автотестах Playwright!

### Преимущества которые вы получили:

- ⚡ **Скорость разработки**: Не нужно писать API вызовы вручную
- 🛡️ **Типобезопасность**: TypeScript защитит от ошибок
- 📦 **Модульность**: Каждый тег в отдельном файле
- 🔄 **Актуальность**: Перегенерируйте при изменении API
- 🌍 **Русский язык**: Поддержка транслитерации

---

**Удачи в автоматизации тестирования! 🚀**

Если возникнут вопросы - загляните в документацию или примеры кода.
